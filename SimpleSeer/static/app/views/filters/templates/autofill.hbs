<h4>{{label}}</h4>
<label for="{{name}}"></label>
<select id="{{name}}">
    <option value="">Select one...</option>
    {{#each constraints.enum}}
    <option value="{{this}}">{{this}}</option>
    {{/each}}
</select>
